@using EntityLayer.Concrete
@model List<Portfolio>

@{
    ViewData["Title"] = "Portfolios";
    Layout = "~/Views/Shared/_NewAdminLayout.cshtml";
}

<div class="main-panel">
    <div class="content-wrapper px-4 py-3">
        <div class="card shadow-sm">
            <div class="card-header bg-gradient-dark text-white d-flex justify-content-between align-items-center">
                <h4 class="mb-0">Portfolios</h4>
                <a asp-action="Add" class="btn btn-success">+ Add New</a>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-dark table-hover">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Title</th>
                                <th>Description</th>
                                <th colspan="2">Image</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.PortfolioId</td>
                                    <td>@item.Name</td>
                                    <td>@item.ProjectUrl</td>
                                    <td>
                                        <img src="@item.ImageUrl1" class="img-thumbnail" style="width: 60px; height: 60px; object-fit: cover;" />
                                    </td>
                                    <td>
                                        <img src="@item.ImageUrl2" class="img-thumbnail" style="width: 60px; height: 60px; object-fit: cover;" />
                                    </td>
                                    <td>
                                        <a asp-action="Update" asp-route-id="@item.PortfolioId" class="btn btn-warning btn-sm">Edit</a>
                                        <a asp-action="Delete" asp-route-id="@item.PortfolioId" class="btn btn-danger btn-sm">Delete</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
